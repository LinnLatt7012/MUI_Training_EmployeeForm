import { makeStyle, withStyles } from '@mui/style'

makeStyle({
    className: {
        style in geneva
    }
})

const classes = styles();
className={classes.sideMenu}

withStyles

    const styles = {
        className{
            style in geneva
        }
    }
    const Sidemenu= (props) => {
        const {classes} =  props;
        return (
            <div className={classes.sideMenu}>  
            </div>
        )
    }
export default withStyles(styles)(Sidemenu)
